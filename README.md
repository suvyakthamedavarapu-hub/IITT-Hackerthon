# IITT-Hackerthon
Test model
Created a separate execution file (test_model.py).  
Loaded the trained model inside the execution file.  
Provided a sample input for testing the model.  
Preprocessed the input data similar to training.  
Used the model to generate predictions.  
Displayed the output result.  
